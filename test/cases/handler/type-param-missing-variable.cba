-- @expect-error: does not handle operation
do h <- return handler {
  $p -> Unique,
  return x -> return x,
  get a k -> k 42,
  ref x k -> k ()
} in
with h handle (
  effect !get : $p ~> Int.
  effect !set : $p * Int ~> Unit.
  effect !ref : Int ~> $p.
  do a <- !ref 0 in
  !set (a, !get a)
)
