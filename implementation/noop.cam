with handler {
  return x -> return x,
  noop(v; k) -> k ()
} handle (
  with handler {
    return x -> return (x, ""),
    print(s; k) -> #noop (); do x <- k () in
      return (fst x, s ++ (snd x))
  } handle (
    #print "A"; #print "B"
  )
)
